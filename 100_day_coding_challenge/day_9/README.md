### Problem Statement

Create a `Todo` class that represents one task.

### Requirements

Properties:

title: string

isCompleted: boolean (initially false)

Methods:

complete() → marks the todo as completed

getStatus() → returns:

"Completed" if done

"Pending" if not done