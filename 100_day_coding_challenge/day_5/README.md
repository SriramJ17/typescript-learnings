Problem Statement

Create a Counter class in TypeScript.

Requirements

The class should have:

value: number (initially 0)

Methods:

increment() → increases value by 1

decrement() → decreases value by 1

getValue() → returns the current value