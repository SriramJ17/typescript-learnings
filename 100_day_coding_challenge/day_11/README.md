Addition of two numbers

This example shows how to add two numbers using the Adder class in add.ts.

Usage:
1. Import or instantiate the Adder class.
2. Call add(a, b) with two numbers.

Example:
const result = new Adder().add(2, 3); // result = 5
