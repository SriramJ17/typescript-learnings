### Problem Statement

Create a TemperatureConverter class.

### Requirements

Methods:

celsiusToFahrenheit(celsius: number): number

fahrenheitToCelsius(fahrenheit: number): number

Each method should:

Take one number as input

Return the converted temperature

### Rules for Today

No properties

No state

No interfaces

No inheritance

Just pure logic inside methods.