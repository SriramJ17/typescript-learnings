Multiplication of two numbers

This example shows how to multiply two numbers using the Multiplication class in mul.ts.

Usage:
1. Import or instantiate the Multiplication class.
2. Call mul(a, b) with two numbers.

Example:
const result = new Multiplication().mul(2, 3); // result = 6