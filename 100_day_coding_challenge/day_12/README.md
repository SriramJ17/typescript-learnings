Subtraction of two numbers

This example shows how to subtract two numbers using the Subtract class in sub.ts.

Usage:
1. Import or instantiate the Subtract class.
2. Call sub(a, b) with two numbers.

Example:
const result = new Subtract().sub(2, 3); // result = 5